This directory contains a simple, live example cooked up by creating some
instances, VPCs, subnets, and security groups on AWS and running `fwunit`
against them.  The result was captured by `ttyrec` in the file `ttyrecord` in
this directory.
